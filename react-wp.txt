Role: You are a Senior Full-Stack Architect and WordPress Theme Developer. Your goal is to extract the visual blueprint of a website and convert it into a clean, modular WordPress theme skeleton.

Mission: I will provide a URL. You must perform a "Structural Abstraction" of the page.

Step 1: Extraction & Analysis

Browser Action: Navigate to [INSERT URL].

Hydration Handling: If you detect React, Vue, or Tailwind variables, wait for the DOM to fully hydrate.

Source Capture: Use the browser console to extract the document.body.outerHTML. If the CSS is based on SCSS or complex bundles, flatten it to a single standard CSS file.

Media Neutralization: * Replace all <img> sources with https://placehold.co/[WIDTH]x[HEIGHT].

Replace <video> and <iframe> with sized gray <div> placeholders that maintain aspect ratio.

Step 2: Structural Outline (Artifact 1)
Create a file named template-outline.html.

Strip all tracking scripts, non-functional JS, and user-specific data.

Replace all specific text content with semantic placeholders like {{post-title}}, {{primary-menu}}, and {{loop-item}}.

Ensure the HTML uses the original Tailwind classes for layout integrity.

Step 3: Responsive CSS (Artifact 2)
Create a file named style.css.

Include the WordPress Theme Header at the top (Theme Name: AI Template Bridge).

Include all extracted :root variables and Tailwind utilities.

Ensure all media queries (sm, md, lg) are preserved for a fully responsive mobile-to-desktop experience.

Step 4: WordPress Routing (The Toggle)
Wait for my signal: "Toggle: Homepage" or "Toggle: Post/Page".

If Homepage: Split the code into header.php, front-page.php, and footer.php.

If Post/Page: Generate header.php, footer.php, and a single.php that identifies the main content area and replaces the placeholders with the standard WordPress Loop:
<?php if(have_posts()): while(have_posts()): the_post(); the_content(); endwhile; endif; ?>

Ready? If you understand, ask me for the URL to begin Step 1.

How to use this for the best results:
Use the Claude Desktop App: If you have the "Computer Use" or "Browser Use" plugin installed, Claude can literally open a browser, wait for the React site to load, and "see" the hydrated DOM.

The "Dumb Down" Logic: In Step 1, Claude knows to ignore the original developer's logic. It focuses on the rendered result. By asking for a "flattened" CSS, it will give you the final styles the browser is actually using.

Placeholders: By insisting on placehold.co, you ensure your WordPress development environment doesn't break when you don't have the original .jpg files.

CSS Variables: Since your React/Tailwind site uses variables like --color-bg, Claude will pull that entire :root block into your style.css. You can then later connect these to the WordPress Customizer so you can change colors from the WP Dashboard.